# v82.10 Changes

- Dynamic button matcher for labels such as `PRIVATE (6)` -> `PRIVATE (23)`.
- Selection-marker normalization for `• Individual` <-> `○ Individual`.
- Button snapshots now retain semantic `match_key` and available callback data.
- Replay prefers recorded callback/position, then semantic label fallback.
- Recorder/dashboard matching now remains stable across dynamic stock and selected-state label changes.
