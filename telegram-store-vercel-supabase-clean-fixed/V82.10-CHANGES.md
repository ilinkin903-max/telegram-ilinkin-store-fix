# v82.10 — Vinnstore spaced stock + delivery wait fix

- Stock Regex contoh Alight Motion sekarang menerima format `[2]. ALIGHT MOTION ( 74 )`.
- `button_index` + `expect_text` menoleransi nomor daftar dan suffix stok `( 74 )`.
- Jika flow order selesai tetapi pesan terakhir masih menu/daftar produk, engine tidak langsung menerapkan Regex Hasil Produk. Engine menunggu sampai ada pesan baru yang benar-benar cocok dengan `delivery_regex` (maks. 45 detik).
- Flow Alight tetap menunggu marker `〔 ACCOUNT DETAIL 〕` sebelum akun dikirim ke pembeli.
- Tidak ada SQL baru.
