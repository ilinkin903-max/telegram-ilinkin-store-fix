v56 AutoGoPay callback verification fix

Result: 48/48 tests passed.
Checks include:
- JavaScript syntax checks for API, library, public, and script files
- Signed AutoGoPay callback verification probe returns HTTP 200
- Unsigned health-check probe cannot process a payment
- Real transaction payload still requires X-Signature
- Existing payment, promo, voucher, marketplace, QRIS, and receipt tests

Note: unit tests used local no-network dependency stubs because the execution environment did not install npm packages. Production dependencies remain unchanged in package.json/package-lock.json and are installed by Vercel.
