# v84.6.0
- Added per-variant shared/separate stock source.
- Shared variants use the product-level stock pool.
- Separate variants use their own stock.
- Mixed modes supported in one product.
- Stock management displays the shared pool once.
- Existing variants retain separate behavior by default.
