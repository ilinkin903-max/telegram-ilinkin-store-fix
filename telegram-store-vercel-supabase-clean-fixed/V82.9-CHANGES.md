# v82.9.0 Changes

- Dynamic quantity button selection for supplier keyboards.
- Recorder highlights buttons already used on the same message.
- Step editor keeps and exposes recorded source buttons for easy replacement after workflow copy.
- Explicit `button_role=quantity` plus runtime auto-detection for legacy workflows.
- Supabase migration adds only `button_role`.
